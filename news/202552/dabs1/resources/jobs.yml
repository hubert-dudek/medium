resources:
  jobs:
    sql_job:
      name: sql_test_job
      tasks:
        - task_key: test_sql
          notebook_task:
            notebook_path: ../src/sql.dbquery.ipynb
            source: WORKSPACE
            warehouse_id: ${simplest_dab.resources.sql_warehouses.my_sql.id}