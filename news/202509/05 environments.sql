-- Databricks notebook source
-- MAGIC %md
-- MAGIC Base environments are custom environment specifications for serverless notebooks that define a serverless environment version and a set of dependencies.
-- MAGIC
-- MAGIC Workspace admins can now create and manage the base environments available in their workspace. They can also set a default base environment for all new serverless notebooks. This allows workspace users to quickly start working in a consistent, cached environment.
-- MAGIC
-- MAGIC See Manage serverless base environments.

-- COMMAND ----------

-- MAGIC %md
-- MAGIC https://docs.databricks.com/aws/en/admin/workspace-settings/base-environment
