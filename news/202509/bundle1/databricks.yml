# This is a Databricks asset bundle definition for bundle1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bundle1
  uuid: 2f0c2d85-014d-47f6-8769-dd33d8fefce2

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3471387803274560.0.azuredatabricks.net
    variables:
      warehouse_id: d56b100f08352714
      catalog: hub
      schema: default

  prod:
    mode: production
    workspace:
      host: https://adb-3471387803274560.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/hubert.dudek@databrickster.com to make sure we only have a single copy.
      root_path: /Workspace/Users/hubert.dudek@databrickster.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: d56b100f08352714
      catalog: hub
      schema: default
    permissions:
      - user_name: hubert.dudek@databrickster.com
        level: CAN_MANAGE
