resources: 
  dashboards:
    sales_overview:
      display_name: "Dashbard with Catalog and Schema changed"
      file_path: ./../src/test_dashboard.lvdash.json
      warehouse_id: ${resources.sql_warehouses.my_sql.id}
      dataset_catalog: ${var.catalog_name}
      dataset_schema: ${var.schema_name}