resources:
  pipelines:
    pipeline_standard:
      name: pipeline_standard
      libraries:
        - glob:
            include: ${workspace.root_path}/files/tests_pipeline/transformations/**
      schema: pipeline_standard
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: tests
      serverless: true
      root_path: ${workspace.root_path}/files/tests_pipeline
      tags:
        "tests": "pipeline_standard"

    pipeline_performance:
      name: pipeline_performance
      libraries:
        - glob:
            include: ${workspace.root_path}/files/tests_pipeline/transformations/**
      schema: pipeline_performance
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: tests
      serverless: true
      root_path: ${workspace.root_path}/files/tests_pipeline
      tags:
        "tests": "pipeline_performance"
